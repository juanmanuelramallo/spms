<%= simple_form_for project do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :url %>
  <%= f.input :client_id, collection: Client.all.map { |client| [client.display_name, client.id] } %>

  <%= f.submit %>
<% end %>
